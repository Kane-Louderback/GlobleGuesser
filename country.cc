#include "country.h"
#include <iostream>
using namespace std;

int getLat(Country a){
    a.lat;
}
int getLon(Country a){
    a.lon;
}

double distance(Country a, Country b){
    int countryAlat = a.lat;
    int countryAlon = a.lon;
    int countryBlat = a.lat;
    int countryBlon = a.lon;

}

